package Exercicio3;

import java.util.Scanner;

public class OperacoesMatematicas {

	public void somar(int numeros[]) {
		
		for(int posicao; posicao < numeros.length; posicao++) {
			
			
		}
		
	}
	
	public void subtrair(int numero[]) {
		
	}
	
	public void multiplicar(int numero[]) {
		
	}
	
	public void dividir(int numUm, int numDois) {
		int dividir = numUm / numDois;
		int modulo = numUm % numDois; 
		System.out.println("Divisão de " + numUm + " por " + numDois + " é " + dividir);
		System.out.println("Modulo de " + numUm + " por " + numDois + " é " + modulo);
	}
	
	public static void main(String[] args) {
		
		Scanner leitura = new Scanner(System.in);
		int numUm = 0, numDois = 0, opcao, tamanho;
		OperacoesMatematicas calculadora = new OperacoesMatematicas();
		int numero[];
		
		while(true) {
			
			System.out.println("Calculadora");
			do {
				System.out.println("Escolha uma das operações abaixo:");
				System.out.println("1) Somar\n2) Subtrair\n3) Multiplicar\n4) Dividir\n5) Sair");
				opcao = Integer.parseInt(leitura.next());
			}while(opcao < 1 || opcao > 6);
			
			if(opcao == 4) {
				
				do {
					System.out.print("Informe o primeiro número: ");
					numUm = Integer.parseInt(leitura.next());
					System.out.print("Informe o segundo número: ");
					numDois = Integer.parseInt(leitura.next());
				}while(numDois == 0);
				
			}else {

				System.out.print("Informe o tamanho do vetor: ");
				tamanho = Integer.parseInt(leitura.next());
				
				int vetor[] = new int[tamanho];
				
				System.out.println();
				for(int posicao; posicao < vetor.length; posicao++) {
					System.out.println("Informe o número para a posição " + (posicao + 1) + ": ");
					vetor[posicao] = Integer.parseInt(leitura.next());
				}
			}
			
			switch(opcao) {
			
				case 1:
					calculadora.somar(numUm, numDois);
					break;
				case 2:
					calculadora.subtrair(numUm, numDois);
					break;
				case 3:
					calculadora.multiplicar(numUm, numDois);
					break;
				case 4:
					calculadora.dividir(numUm, numDois);
					break;
				case 5:
					System.exit(0);
					break;
				default:
					System.out.println("Opção informava inválida!");
					break;
			}
			
		}
		
	}
}
